# Codeception Test Suite Configuration
#
# Suite for api tests.
actor: ApiTester
path: .
modules:
    enabled:
        - REST:
            url: http://localhost:8000/api
            depends: PhpBrowser
